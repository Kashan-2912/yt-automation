# DISABLED — channel preset 'hindi_myth' no longer exists.
# This workflow is kept for reference only. Delete if not needed.
name: "[DISABLED] Daily Hindi Myth Short"

on:
  workflow_dispatch:
    inputs:
      note:
        description: "This workflow is disabled. Use daily_short.yml instead."
        required: false
        default: ""

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is disabled. The active channel is 'football'. See daily_short.yml."
